# PATH Social - RuVector PostgreSQL Configuration
# Copy this file to .env and customize for your environment

# Database connection
DATABASE_URL=postgres://path_user:path_secure_password@localhost:5432/path_social

# Connection pool settings
DATABASE_MAX_CONNECTIONS=10
DATABASE_TIMEOUT=30

# Vector embedding dimension (must match HNSW index configuration)
EMBEDDING_DIMENSION=4096

# Optional: For production, use environment-specific credentials
# POSTGRES_USER=path_user
# POSTGRES_PASSWORD=your_secure_password
# POSTGRES_DB=path_social
